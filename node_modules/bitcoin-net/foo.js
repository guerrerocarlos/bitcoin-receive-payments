var PeerGroup = require('./').PeerGroup
var params = Object.assign(require('webcoin-bitcoin').net, {
  webSeeds: [ 'ws://localhost:8192' ],
  dnsSeeds: []
})

var wrtc = require('wrtc')

var peers = new PeerGroup(params, { wrtc, connectWeb: true })
peers.connect()
